import { SimpleTextWithSetterDirective } from './simple-text-with-setter.directive';

describe('SimpleTextWithSetterDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleTextWithSetterDirective();
    expect(directive).toBeTruthy();
  });
});
