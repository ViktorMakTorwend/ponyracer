import { SameNameInputDirective } from './same-name-input.directive';

describe('SameNameInputDirective', () => {
  it('should create an instance', () => {
    const directive = new SameNameInputDirective();
    expect(directive).toBeTruthy();
  });
});
