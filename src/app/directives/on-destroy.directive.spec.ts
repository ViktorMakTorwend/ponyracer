import { OnDestroyDirective } from './on-destroy.directive';

describe('OnDestroyDirective', () => {
  it('should create an instance', () => {
    const directive = new OnDestroyDirective();
    expect(directive).toBeTruthy();
  });
});
